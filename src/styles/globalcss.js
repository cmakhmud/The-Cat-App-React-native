import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
    app:{
        display:'flex',
        justifyContent:'center',
        alignItems:'center'
    }
})